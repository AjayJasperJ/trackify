import 'package:trackify/widgets/form_section_card.dart';

/// Backwards-compatible alias so existing goal widgets keep working.
/// Design now lives in the shared [FormSectionCard].
typedef CreateGoalSectionCard = FormSectionCard;
